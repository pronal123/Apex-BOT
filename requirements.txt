# ====================================================================
# Apex BOT v12.1.36 - FINAL_CONV_PARALLEL 実行環境に必要なライブラリ
# ====================================================================

# 仮想通貨取引所との連携（非同期対応クライアント）
ccxt==4.2.56  # 最新の安定版またはご使用の環境に合わせたバージョン
ccxt.async_support  # ccxtに含まれるため、別途記載は不要ですが、動作確認のためバージョンは明記

# データ処理と分析
pandas>=2.0.0
numpy>=1.24.0

# テクニカル分析ライブラリ
pandas-ta>=0.3.14b0

# 非同期処理とWeb API（BOTの実行とステータス表示用）
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
python-dotenv>=1.0.0  # 環境変数（.envファイル）の読み込み用

# APIとの通信
requests>=2.31.0

# --------------------------------------------------------------------
# 注意事項: 
# - 上記バージョンは推奨値です。環境により最新版や特定バージョンが
#   必要となる場合があります。
# - macOS/Windows環境でasyncio関連のエラーが出る場合は、
#   別途 `nest_asyncio` のインストールが必要になることがあります。
#   (例: nest_asyncio>=1.5.8)
# --------------------------------------------------------------------
